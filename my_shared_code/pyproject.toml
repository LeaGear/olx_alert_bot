[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "my_shared"
version = "0.1.0"
description = "Общий код для бота, сервера и парсера"
dependencies = ["pydantic", "functools", "httpx"] # Dependencies imports